<context>
{% for chunk in chunks %}{% set citable = chunk.get("citable", True) %}<source id="{{ loop.index }}"{% if not citable %} citable="false"{% endif %}{% if chunk.get("title") %} title="{{ chunk.get("title") | e }}"{% endif %}>{{ chunk.text | e }}</source>
{% endfor %}</context>
