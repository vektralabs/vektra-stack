[tool.uv.workspace]
members = ["vektra-shared", "vektra-core", "vektra-ingest", "vektra-index"]

[dependency-groups]
dev = [
    "alembic>=1.13",
    "testcontainers[postgres]>=4.8",
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
]
